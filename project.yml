name: Anygram
options:
  projectFormat: xcode15
  bundleIdPrefix: com.anygram
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "15.0"
  createIntermediateGroups: true
  groupSortPosition: top
  developmentLanguage: ru

packages:
  TDLibKit:
    url: https://github.com/Swiftgram/TDLibKit
    revision: 245888f853f5e304029f4fa423af14a045476f30

settings:
  base:
    SWIFT_VERSION: "5.10"
    TARGETED_DEVICE_FAMILY: "1,2"
    SUPPORTS_MACCATALYST: false
    ENABLE_USER_SCRIPT_SANDBOXING: false
    SWIFT_EMIT_LOC_STRINGS: YES
    DEVELOPMENT_LANGUAGE: ru

targets:
  Anygram:
    type: application
    platform: iOS
    sources:
      - path: Anygram
        excludes:
          - "**/.DS_Store"
    dependencies:
      - package: TDLibKit
        product: TDLibKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.anygram.app
        PRODUCT_NAME: Anygram
        INFOPLIST_KEY_CFBundleDisplayName: Anygram
        INFOPLIST_KEY_CFBundleDevelopmentRegion: ru
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: true
        INFOPLIST_KEY_UILaunchScreen_Generation: true
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone: UIInterfaceOrientationPortrait
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        CURRENT_PROJECT_VERSION: 1
        MARKETING_VERSION: 1.0.0
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        DEVELOPMENT_TEAM: ""
        OTHER_LDFLAGS: "$(inherited) -lc++ -lz"
    info:
      path: Anygram/Resources/Info.plist
      properties:
        CFBundleDisplayName: Anygram
        CFBundleDevelopmentRegion: ru
        UILaunchScreen: {}

schemes:
  Anygram:
    build:
      targets:
        Anygram: all
    run:
      config: Debug
    archive:
      config: Release
